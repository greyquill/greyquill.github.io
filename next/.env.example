# Web3Forms access key for the /contact form. Public-safe (bound to the
# destination email at web3forms.com, not a secret credential).
# Get one at https://web3forms.com (registered to amarnath@greyquill.io).
# For production, set this as a GitHub Actions secret with the same name
# so the build step can inline it.
NEXT_PUBLIC_WEB3FORMS_KEY=
